<Window x:Class="SEM_1_GUI.RegionCounts"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SEM_1_GUI"
        xmlns:models="clr-namespace:SEM_1.Core;assembly=SEM_1.Core" 
        mc:Ignorable="d"
        Title="RegionCounts" Height="450" Width="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" MinHeight="34"/>
                <RowDefinition Height="Auto" MinHeight="34"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0">
        <Run Text="Regions count: " FontWeight="Bold"/><Run Text="{Binding RegionCount}"/>
            </TextBlock>
            <ListBox x:Name="Region" Grid.Row="1" ItemsSource="{Binding Regions}" Grid.ColumnSpan="1" Margin="5,3" VerticalAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:Region}">
                        <Grid Margin="5">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0">
                        <Run Text="Region ID: " FontWeight="Bold"/><Run Text="{Binding ID}"/>
                            </TextBlock>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </ScrollViewer>
</Window>
